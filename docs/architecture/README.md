# Arquitectura

Esta carpeta documenta el sistema existente y su dirección aprobada. `system-overview.md` explica límites; `module-map.md` es además un contrato consumido por CI; `api-contracts.md` documenta las funciones PostgreSQL expuestas mediante `GRANT EXECUTE`.

Una modificación estructural requiere actualizar estos documentos en el mismo PR.
